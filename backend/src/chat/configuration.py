# =============================================================================
# LangGraph 聊天代理 - 混合模型配置模块（共享版本）
# =============================================================================
# 本文件现在使用共享的配置模块，保持向后兼容的接口：
# 1. 导入共享的配置功能
# 2. 提供向后兼容的类别名
# 3. 保持原有的 API 接口不变
# 4. 统一配置管理方式
# =============================================================================

# 导入共享的配置功能
from src.shared.configuration import (
    ChatConfiguration as SharedChatConfiguration,
    Configuration as SharedConfiguration
)

# LangChain 运行时配置（保持兼容性）
from langchain_core.runnables import RunnableConfig


# =============================================================================
# 向后兼容的类别名
# =============================================================================
# 为了保持向后兼容性，提供原有的类名别名
# =============================================================================

# 使用共享的配置类作为 ChatConfiguration
ChatConfiguration = SharedChatConfiguration